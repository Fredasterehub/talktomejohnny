[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "talktomejohnny"
version = "0.1.0"
description = "A local-first voice medium for Claude Code and Codex CLI."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
    "click>=8.1",
    "numpy>=1.24",
    "piper-tts>=1.5.0",
    "sounddevice>=0.5",
    "faster-whisper>=1.2",
    "huggingface_hub>=0.24",
    "textual>=8.2",
]

[project.optional-dependencies]
cuda = [
    "nvidia-cublas-cu12",
    "nvidia-cudnn-cu12>=9",
]

[project.scripts]
talktomejohnny = "talktomejohnny.cli:main"
talktomeclaude = "talktomeclaude.cli:main"

[project.urls]
Homepage = "https://github.com/Fredasterehub/talktomejohnny"
Repository = "https://github.com/Fredasterehub/talktomejohnny"
Issues = "https://github.com/Fredasterehub/talktomejohnny/issues"

[tool.setuptools.packages.find]
where = ["src"]
