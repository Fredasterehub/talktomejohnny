{
  "session_id": "synthetic-session",
  "transcript_path": "tests/fixtures/transcript.jsonl",
  "cwd": "/workspace/example",
  "permission_mode": "default",
  "hook_event_name": "Stop",
  "stop_hook_active": false,
  "last_assistant_message": "The synthetic fixture is valid. I also checked the guard helper:\n\n```python\ndef forbidden_function(path):\n    raise RuntimeError(\"never call this from tests\")\n```\n\nAll twelve tests pass now. The fixture contains no operator data."
}
